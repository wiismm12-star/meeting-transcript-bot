# 貢獻指南

感謝協助改善本專案。

## 開發流程

1. 從 `master` 建立功能分支。
2. 保持變更範圍聚焦，並為行為變更補上測試。
3. 執行完整測試：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

4. 不要提交 `.env`、API Key、Token、音檔、逐字稿、SQLite 資料庫或 `data/` 內容。
5. 提交 Pull Request 時，說明使用情境、測試結果與使用者可見的變更。

## 設計原則

- 介面與錯誤訊息採繁體中文。
- 不猜測或宣稱辨識到主講人的真實身份。
- 逐字稿潤稿必須保守，不得編造決議、行動或內容。
- 新增雲端服務時，須明確說明資料會傳送到哪裡及所需憑證。
